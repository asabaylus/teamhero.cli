import { describe, it } from "bun:test";

describe("report command", () => {
	it.skip("CLI delegates to Go TUI binary - integration tests need TUI binary present", () => {
		// The CLI delegates to the Go TUI binary for report generation.
		// Integration tests require the TUI binary to be built.
	});
});
