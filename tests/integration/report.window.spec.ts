import { describe, it } from "bun:test";

describe("report window filtering", () => {
	it.skip("CLI delegates to Go TUI binary - window filtering handled by TUI", () => {
		// Window filtering is handled by the Go TUI and TypeScript service
	});
});
